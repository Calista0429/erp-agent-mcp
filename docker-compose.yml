services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?run `npm run init-env` to create .env}
      POSTGRES_DB: ${PGDATABASE:-erp}
    ports:
      - "${PGPORT:-55432}:5432" # 55432 to avoid clashing with a local Postgres
